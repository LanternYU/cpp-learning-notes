# 五。补充内容
## 5.1 pointer-like clss 关于智能指针
### 5.1.1 shared_prt
```cpp
template<class T>
class shared_ptr
{
private:
    T* px;
    long* pn;
public:
    T& operator*() const
    {return *px;}
    T& operator->() const
    {return px;}
    shared_ptr(T* p):px(p){ }
}
```
